const initState = {
  articleList: null,
  currentOpenArticle: null,
};

const article = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default article;
